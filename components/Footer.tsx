import { HStack, Text } from '@chakra-ui/react';

export const Footer = () => (
	<HStack mt={10} w={'full'}>
		<Text w={'full'} fontSize={'sm'} textAlign={'center'}>
			Made with ❤️ by drgato
		</Text>
	</HStack>
);
